# AI模型评测Web系统 - 可选依赖配置

# 性能分析功能 [perf]
# 若要使用性能分析和压测功能，需安装以下依赖：
psutil==5.9.5
memory-profiler==0.61.0
line-profiler==4.1.1

# 可视化功能 [viz]
# 若要使用高级图表和可视化功能，需安装以下依赖：
matplotlib==3.7.2
seaborn==0.12.2
plotly==5.15.0

# 开发调试功能 [dev]
# 若要进行开发和调试，需安装以下依赖：
jupyter==1.0.0
ipython==8.14.0
pytest==7.4.0
pytest-cov==4.1.0
black==23.7.0
flake8==6.0.0

# 高级分析功能 [analytics]
# 若要使用高级统计分析功能，需安装以下依赖：
scipy==1.11.1
scikit-learn==1.3.0
networkx==3.1

# 多语言支持 [i18n]
# 若要支持多语言界面，需安装以下依赖：
babel==2.12.1
flask-babel==3.1.0

# 数据导出功能 [export]
# 若要支持更多数据导出格式，需安装以下依赖：
xlsxwriter==3.1.2
reportlab==4.0.4

# 并发处理功能 [async]
# 若要使用高级并发处理功能，需安装以下依赖：
asyncio==3.4.3
uvloop==0.17.0

# 缓存功能 [cache]
# 若要使用Redis缓存功能，需安装以下依赖：
redis==4.6.0
flask-caching==2.0.2

# 安全功能 [security]
# 若要增强安全功能，需安装以下依赖：
cryptography==41.0.3
flask-limiter==3.3.1
flask-cors==4.0.0

# 监控功能 [monitoring]
# 若要使用系统监控功能，需安装以下依赖：
prometheus-client==0.17.1
flask-prometheus-metrics==1.0.0

# 安装命令示例：
# pip install -r requirements.txt                    # 基础功能
# pip install -r requirements.txt -r requirements-optional.txt  # 全部功能
# 
# 按需安装：
# pip install psutil memory-profiler                 # 仅性能分析
# pip install matplotlib seaborn plotly             # 仅可视化
# pip install jupyter ipython pytest                # 仅开发调试
